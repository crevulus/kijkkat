export enum NavigationRoutes {
  Home = "/",
  Map = "/map",
  Add = "/add",
  Posts = "/posts",
  PostsDynamic = "/posts/:id",
  Account = "/account",
}

export enum PageNames {
  Home = "Home",
  Map = "Map",
  Add = "Add",
  Posts = "Posts",
  Account = "Account",
}
